# .github/workflows/ci.yml
#
# NOTE: These checks run on every PR but do NOT block merges yet.
# Blocking requires branch protection, which requires GitHub Pro (private repo)
# or making the repo public. When you're ready:
#   → Run scripts/setup-branch-protection.sh (instructions inside)
#
name: CI

on:
  pull_request:
    branches: [main]
    paths-ignore:
      - "**.md"
      - "CHANGELOG.md"
      - "docs/**"
      - ".github/ISSUE_TEMPLATE/**"

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ==========================================
  # JOB 1: Lint + Type Check + Tests + Build
  # One npm ci. Fast fail order: lint → types → tests → build → audit.
  # ==========================================
  check:
    name: Lint, Types, Test, Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm install -g npm@11
      - run: npm ci
      - run: npm run lint
      - run: npx astro check
      - run: npm test
      - run: npm run build
      - name: Security audit
        # --audit-level=critical: unfixed HIGH vulns are build-tool internals only
        #   (esbuild dev-server Windows-only GHSA-g7r4-m6w7-qqqr in @astrojs/react;
        #    path-to-regexp ReDoS GHSA-9wv6-86v2-598j in @astrojs/vercel routing utils)
        # Neither processes user input at runtime. Audit still reports them for visibility.
        # --omit=dev: exclude CI/dev tooling (@lhci/cli, firebase-tools)
        run: npm audit --audit-level=critical --omit=dev

  # Playwright smoke tests moved to local pre-push hook (~/.git-hooks/pre-push).
  # This eliminates the primary GitHub Actions minute consumer.
  # Production coverage: smoke-tests.yml runs weekly against all 4 viewports on schedule.
