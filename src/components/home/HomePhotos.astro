---
import {
  GALLERY_PHOTOS,
  GALLERY_HEADING,
  GALLERY_SUBHEADING,
} from "@/data/gallery";
---

<section class="photos" id="gallery" aria-label="Show gallery">
  <div class="photos-header" data-reveal>
    <h2>{GALLERY_HEADING}</h2>
    <p>{GALLERY_SUBHEADING}</p>
  </div>
  <div class="photo-grid">
    {
      GALLERY_PHOTOS.map((photo) => (
        <div class:list={["photo", { "photo--wide": photo.span }]} data-reveal>
          <img
            src={`/images/${photo.src}`}
            alt={photo.alt}
            width={photo.w}
            height={photo.h}
            loading="lazy"
            style={`--photo-pos: ${photo.pos};`}
          />
          <div class="photo-label">{photo.label}</div>
        </div>
      ))
    }
  </div>
</section>

<script>
  import { trackLeadEvent } from "@/lib/analytics";

  const gallery = document.getElementById("gallery");
  if (gallery) {
    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting) {
          trackLeadEvent("gallery_viewed", {
            photo_count: gallery.querySelectorAll(".photo").length,
            page: window.location.pathname,
          });
          observer.disconnect();
        }
      },
      { threshold: 0.3 },
    );
    observer.observe(gallery);
  }
</script>

<style>
  .photos {
    padding: 60px 16px;
    background: var(--charcoal);
  }
  .photos-header {
    max-width: 1200px;
    margin: 0 auto 36px;
  }
  .photos-header h2 {
    font-family: var(--font-playfair), serif;
    font-size: clamp(32px, 5vw, 60px);
    font-weight: 900;
    color: white;
    line-height: 1;
  }
  .photos-header p {
    color: rgba(255, 255, 255, 0.4);
    font-size: 14px;
    margin-top: 8px;
  }
  .photo-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 6px;
    max-width: 1200px;
    margin: 0 auto;
  }
  .photo {
    border-radius: 12px;
    display: flex;
    align-items: flex-end;
    justify-content: flex-start;
    position: relative;
    overflow: hidden;
    transition:
      transform 0.35s cubic-bezier(0.34, 1.3, 0.64, 1),
      filter 0.3s;
    aspect-ratio: 1;
    padding: 10px;
  }
  .photo img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: var(--photo-pos, center);
    display: block;
  }
  .photo:hover {
    transform: scale(1.025);
    filter: brightness(1.08);
  }
  .photo--wide {
    display: none;
  }
  .photo-label {
    position: relative;
    z-index: 1;
    color: white;
    font-size: 10px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 1.5px;
    background: rgba(0, 0, 0, 0.45);
    backdrop-filter: blur(6px);
    padding: 5px 12px;
    border-radius: 50px;
  }

  @media (min-width: 768px) {
    .photos {
      padding: 100px 48px;
    }
    .photos-header {
      margin-bottom: 48px;
    }
    .photo-grid {
      grid-template-columns: repeat(4, 1fr);
      grid-template-rows: 300px 220px;
      gap: 10px;
    }
    .photo {
      aspect-ratio: auto;
    }
    .photo--wide {
      display: flex;
      grid-column: span 2;
      grid-row: span 2;
    }
    .photo-label {
      font-size: 11px;
    }
  }
</style>
