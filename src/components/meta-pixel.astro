---
// src/components/meta-pixel.astro — Facebook Meta Pixel (1469248418329402)
// Deferred: loads after page is idle to avoid blocking LCP
---

<script is:inline>
  function loadMetaPixel() {
    if (window._fbLoaded) return;
    window._fbLoaded = true;
    !(function (f, b, e, v, n, t, s) {
      if (f.fbq) return;
      n = f.fbq = function () {
        n.callMethod
          ? n.callMethod.apply(n, arguments)
          : n.queue.push(arguments);
      };
      if (!f._fbq) f._fbq = n;
      n.push = n;
      n.loaded = !0;
      n.version = "2.0";
      n.queue = [];
      t = b.createElement(e);
      t.async = !0;
      t.src = v;
      s = b.getElementsByTagName(e)[0];
      s.parentNode.insertBefore(t, s);
    })(
      window,
      document,
      "script",
      "https://connect.facebook.net/en_US/fbevents.js",
    );
    fbq("init", "1469248418329402");
    fbq("track", "PageView");
  }
  if (window.__GMD_CONSENT?.marketing) {
    if ("requestIdleCallback" in window) {
      requestIdleCallback(loadMetaPixel, { timeout: 3000 });
    } else {
      setTimeout(loadMetaPixel, 2000);
    }
  }
  window.addEventListener("gmd:consent-updated", function (e) {
    if (e.detail && e.detail.marketing) loadMetaPixel();
  });
</script>
